#include <stdlib.h>
#include <stdio.h>

/**
 * mod - finds modulus of two integers
 * @a: integer one
 * @b: integer two
 * Return: the result of modulus
 */
int mod(int a, int b)
{
	return (a % b);
}
