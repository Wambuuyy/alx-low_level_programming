#include <stdlib.h>
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: integer one
 * @b: integer two
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
